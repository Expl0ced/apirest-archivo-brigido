import * as MF from '../types/System';
import { Client } from '../lib/client';
export declare class System extends Client {
    host?: string | undefined;
    token?: string | undefined;
    constructor(host?: string | undefined, token?: string | undefined);
    /**
     * Returns the current state of the cloud infrastructure.
     */
    getStatus(): Promise<MF.SystemStatus>;
    /**
     * Returns a list of various limits that the API honors.
     */
    getLimits(): Promise<MF.SystemLimits>;
}
